---
name: junie-config-checker
description: Check managed Junie CLI config.json for target scope, schema, and safety.
---

# Junie Config Checker

Use this skill when reviewing managed Junie `config.json`.

Read `../nddev-builder/references/native-path-model.md` and
`../nddev-builder/references/config.md` first.

Verify the JSON object is manager-owned, target-scoped, secret-free, and aligned
with the selected permission profile.
